const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('#site-nav');menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));document.querySelectorAll('.service-toggle').forEach(button=>button.addEventListener('click',()=>{const row=button.closest('.service-row');const open=row.classList.toggle('open');button.setAttribute('aria-expanded',open);button.querySelector('span').textContent=open?'−':'+'}));const dialog=document.querySelector('#led-dialog');const serviceSelect=document.querySelector('#service-select');let acknowledged=false;function seatingNotice(){if(!acknowledged&&serviceSelect.value.includes('Guest Seating'))dialog.showModal()}serviceSelect.addEventListener('change',seatingNotice);document.querySelectorAll('[data-service]').forEach(button=>button.addEventListener('click',()=>{serviceSelect.value=button.dataset.service;setTimeout(seatingNotice,450)}));document.querySelector('.dialog-close').addEventListener('click',()=>{serviceSelect.value='';dialog.close()});document.querySelector('.dialog-confirm').addEventListener('click',()=>{acknowledged=true;dialog.close();serviceSelect.focus()});const form=document.querySelector('#inquiry-form');form.addEventListener('submit',async event=>{event.preventDefault();if(serviceSelect.value.includes('Guest Seating')&&!acknowledged){seatingNotice();return}const submit=form.querySelector('[type=submit]');submit.disabled=true;submit.textContent='Sending…';try{await fetch('/',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams(new FormData(form)).toString()});form.reset();document.querySelector('.toast').classList.add('show');setTimeout(()=>document.querySelector('.toast').classList.remove('show'),6000)}catch(error){form.submit()}finally{submit.disabled=false;submit.innerHTML='Send my inquiry <span>↗</span>'}});document.querySelector('#year').textContent=new Date().getFullYear();
